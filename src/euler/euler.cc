#include "euler.h"
#include <cmath>  // sqrt
#include <complex> // complex sqrt

/*
void roeSolver(int numDofPerNode, double* qL, double* qR, double* nrm, double* res)
{

  // this works ok, sometimes
  for (int i=0; i < numDofPerNode; ++i)
    nrm[i] = 0.5*(qL[i] + qR[i]);
}
*/


